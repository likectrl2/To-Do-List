export default function AppEntriesManager({className}: {className: string}) {
    return (
        <div
            className={`${className}`}
        >
            AppEntriesManager
        </div>
    )
}